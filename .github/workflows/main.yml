name: demo-actions

on:
  # push:
  #   branches: main
  issues:
    types: opened
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4.2.2

      - name: run a sample
        run: echo "hello world"
